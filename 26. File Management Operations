#include<stdio.h>

int main()
{
    FILE *fp;

    fp=fopen("test.txt","w");

    fprintf(fp,"File Management");

    fclose(fp);

    fp=fopen("test.txt","r");

    char str[50];

    fgets(str,50,fp);

    printf("%s",str);

    fclose(fp);

    return 0;
}
